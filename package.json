{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express.js application that emulates accelbyte's backend used by splitgate",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun x tsc",
    "assets": "if exist \"dist\\\" (cp -R assets dist) else (bun run build && exit 1)",
    "start": "bun run assets && bun ."
  },
  "keywords": [
    "splitgate",
    "backend",
    "api"
  ],
  "author": "PADO",
  "packageManager": "bun@1.12.0",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/bun": "latest",
    "@types/ws": "^8.5.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "express": "^4.18.1",
    "hono": "^4.8.12",
    "ws": "^8.18.3"
  }
}
